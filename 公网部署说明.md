# 数字乱斗公网联机版

这个版本会把网页和联机服务器部署在同一个公网地址。玩家打开同一网址后，房主创建房间并分享邀请码或邀请链接，异地玩家即可加入。

## 发布到 Render

1. 解压部署包，把“数字乱斗-公网联机部署版”文件夹里面的全部内容上传到一个 GitHub 仓库根目录。`render.yaml` 必须直接位于仓库根目录，不要只上传 ZIP。
2. 登录 Render，选择 `New` → `Blueprint`。
3. 连接该 GitHub 仓库，Render 会读取根目录下的 `render.yaml`。
4. 确认创建 `number-brawl-online` 服务，等待构建完成。
5. 打开 Render 给出的 `https://...onrender.com` 地址，即可创建公网房间。

不需要额外填写端口。服务会自动读取部署平台提供的 `PORT`，邀请链接也会自动使用公网 HTTPS 地址。

## 联机规则

- 最多 15 名角色；真人不足时由人机补齐。
- 临时断网后会自动重连，服务器为玩家保留 20 秒位置。
- 房主离开后会自动把房主交给下一位在线玩家，对局不会立即解散。
- 当前房间保存在单台服务器内存中，因此服务实例数量必须保持为 1。

## 本机测试

先运行 `npm run build`，再运行 `npm run start:local`。浏览器会自动打开本机联机页面。
